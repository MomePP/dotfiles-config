# vim: ft=tmux

# if-shell 'test -z "$POWERLINE_CONFIG_COMMAND"' 'set-environment -g POWERLINE_CONFIG_COMMAND powerline-config'

# Don't version-check for this core functionality -- anything too old to
# support these options likely won't work well with powerline
# Keep this `on`: tmux-continuum runs its autosave via a #() interpolation in
# status-right, which tmux only evaluates when it draws the status line.
# Turning this off silently disables continuum's 15-minute autosave.
set -g status on
set-option -g status-style bg=default
#
# set -g status-left-length 60
# set -g status-left '#[fg=colour235,bg=colour252,bold] #S #[fg=colour244,bg=default] '
#
# set -g status-right-length 150
# set -g status-right '#[fg=colour252,bg=colour238,bold] continuum: #{continuum_status}#[fg=colour255,bg=colour238] #[fg=colour16,bg=colour254,bold] #h '
#
# set -g window-status-separator '#[fg=colour244,bg=default] '
# set -g window-status-format "#[default] #I  #W "
# set -g window-status-current-format "#[fg=colour117,bg=colour31,bold] #I  #[fg=colour231]#W "

# INFO: gruvbox light theme
# set -g status-left-length 85
# set -g status-left "#[fg=colour236] working on#[fg=colour135,bold] #S"
# set -g window-status-current-format "#[fg=colour135,bold] #W "
# set -g window-status-format "#[default] #W "
# set -g status-style bg=default
# set -g status-right "#[fg=colour135] %a │#[fg=colour236] %H:%M %p #[fg=colour135] %d %b %Y "
# set -g status-justify centre

# INFO: gruvbox dark theme
# set -g status-left-length 85
# set -g status-left "#[fg=colour7] working on#[fg=colour3,bold] #S"
# set -g window-status-current-format "#[fg=colour3,bold] #W "
# set -g window-status-format "#[default] #W "
# set -g status-style bg=default
# set -g status-right "#[fg=colour3] %a │#[fg=colour7] %H:%M %p #[fg=colour3] %d %b %Y "
# set -g status-justify centre

# INFO: kanagawa theme
# set -g status-left-length 85
# set -g status-left "#[fg=colour255] working on#[fg=colour135,bold] #S"
# set -g window-status-current-format "#[fg=colour135,bold bg=colour235] #W "
# set -g window-status-format "#[default] #W "
# set -g status-style bg=default
# set -g status-right "#[fg=colour135] %a │#[fg=colour255] %H:%M %p #[fg=colour135] %d %b %Y "
# set -g status-justify centre

# INFO: tokyonight theme
# set -g status-left-length 85
# set -g status-left "#[fg=colour15] working on#[fg=colour6,bold] #S"
# set -g window-status-current-format "#[fg=colour6,bold] #W "
# set -g window-status-format "#[default] #W "
# set -g status-style bg=default
# set -g status-right "#[fg=colour6] %a │#[fg=colour15] %H:%M %p #[fg=colour6,bold] %d %b %Y "
# set -g status-justify centre

# INFO: catppuccin latte theme
# set -g status-left-length 85
# set -g status-left "#[fg=colour0] working on#[fg=colour4,bold] #S"
# set -g window-status-current-format "#[fg=colour4,bold] #W "
# set -g window-status-format "#[default] #W "
# set -g status-style bg=default
# set -g status-right "#[fg=colour4,bold] %a │#[fg=colour0] %H:%M %p #[fg=colour4,bold] %d %b %Y "
# set -g status-justify centre

# INFO: catppuccin mocha theme
# set -g status-left-length 85
# set -g status-left "#[fg=colour7] working on#[fg=colour4,bold] #S"
# set -g window-status-current-format "#[fg=colour4,bold] #W "
# set -g window-status-format "#[default] #W "
# set -g status-style bg=default
# set -g status-right "#[fg=colour4,bold] %a │#[fg=colour7] %H:%M %p #[fg=colour4,bold] %d %b %Y "
# set -g status-justify centre

# INFO: oxocarbon theme -- colors are the real palette from momepp/oxocarbon.nvim
# rather than approximated ANSI slots. ACCENT is `lavender`; swap it in one place
# to retheme the whole bar. Other purples in the fork: base14 #be95ff,
# periwinkle #b4befe, indigo #818cf8.
%hidden ACCENT="#cba6f7"
%hidden PINK="#ff7eb6"
%hidden DIM="#525252"
%hidden BORDER="#393939"

set -g status-position bottom
set -g status-style bg=default
set -g status-justify centre

# Pane borders. BORDER is base02, the same value nvim uses for WinSeparator, so
# a tmux split and an nvim split draw the same weight of line.
#
# The active border is deliberately just one step up the greyscale (base02 ->
# base03) rather than an accent colour. A split is a constant, ambient piece of
# chrome; colouring it lavender made it compete with the status line for
# attention. If the distinction is too faint, `ghost` #808080 is the next step.
#
# status-left and status-right MUST stay the same rendered width (37 columns).
# tmux centres the window list in the space BETWEEN left and right, not on the
# screen, so unequal sides drag the middle sideways -- each side shifts it by
# half the difference. Equal widths pin the middle to true screen centre.
#
#   left  = " working on " (12) + session field (25)              = 37
#   right = phrase block (31) + state word (5) + " " (1)          = 37
#           phrase = " running " (9) + command (13) + ", prefix " (9)
#
# Both sides read as sentences, so the phrase must stay tight -- padding the
# command directly would strand the comma out in whitespace. Instead the whole
# phrase is right-aligned with #{p-N:...}, which pads on the LEFT (undocumented
# but real), putting all the slack ahead of "running" where it is just a gap.
#
# The pad is 57, not 31: #[...] style tags count toward the pad budget, and the
# phrase carries two inline tags of 13 characters each. 31 + 26 = 57. Change a
# colour to anything but a 7-char hex and this number moves with it.
#
# The state word is deliberately OUTSIDE the pad. Its two branches carry style
# tags of different lengths (#[fg=#ff7eb6,bold] is 18, #[fg=#525252] is 13), so
# inside the pad the armed state would render 5 columns narrower than the idle
# one. Keep any replacement pair the same VISIBLE length: ready/armed are both 5.
#
# Fields are truncate-then-pad: #{p17:...} alone does NOT truncate, so a long
# name would overflow and break the balance. Chaining as #{p17:=17:...} silently
# yields blanks -- the inner format must be nested as #{p17:#{=17:...}}.
set -g status-left-length 40
set -g status-left "#[fg=${DIM}] working on#[fg=${PINK},bold] #{p25:#{=25:session_name}}"

# The agent dots ride the window list, not either edge. They are the one field
# here whose width is genuinely variable -- nothing, one dot, or one per agent
# in the window -- and the centre is the only section that can absorb that
# without moving anything else. The conditional keeps the leading space out when
# a window has no agent, so an ordinary shell window is unchanged.
set -g window-status-format "#[fg=${DIM}] #W#{?@tmux_agent_dock_window_agents, #{@tmux_agent_dock_window_agents},} "
set -g window-status-current-format "#[fg=${ACCENT},bold] #W#{?@tmux_agent_dock_window_agents, #{@tmux_agent_dock_window_agents},} "

# Right side shows what tmux surfaces nowhere else: prefix state is invisible by
# default, and the zoom flag is hidden because window-status-format omits #F.
# No clock (menu bar has it) and no hostname (single machine) -- both were filler.
#
# Both labels ALWAYS render and only change colour -- never conditional text.
# status-justify is `centre`, so tmux centres the window list in the space left
# over; a right side that changed width would drag the middle section sideways.
set -g status-right-length 40
# "running" names the agent when the window has one and the foreground command
# otherwise -- `running claude` says more than `running nu`, which is only ever
# the shell the agent was launched through. The swap is safe for the balance
# above: it adds no style tags, so the 57 budget is unchanged, and the visible
# length was already variable and already absorbed by the left-pad.
set -g status-right "#[fg=${DIM}]#{p-57:#{ running #[fg=${ACCENT}]#{=13:#{?@tmux_agent_dock_window_cli,#{@tmux_agent_dock_window_cli},#{pane_current_command}}}#[fg=${DIM}], prefix }}#{?client_prefix,#[fg=${PINK}#,bold]armed,#[fg=${DIM}]ready} "

# load powerline
# if-shell 'env "$POWERLINE_CONFIG_COMMAND" tmux setup' '' 'run-shell "powerline-config tmux setup"'
