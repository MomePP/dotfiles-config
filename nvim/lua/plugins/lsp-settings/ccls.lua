return {
    init_options = {
        cache = {
            directory = ".ccls-cache"
        },
        -- index = {
        --     multiVersion = 1,
        --     multiVersionBlacklist = { "^/usr/include" },
        -- }
    }
}
